{
  "name": "moleculens",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "pnpm --filter @moleculens/web dev",
    "build:web": "pnpm --filter @moleculens/web build",
    "lint": "echo ok"
  }
}
